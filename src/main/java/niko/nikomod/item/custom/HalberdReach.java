package niko.nikomod.item.custom;

public final class HalberdReach {
    /** Extra distance to allow beyond vanilla feel (≈4.5 survival / 5.0 creative). */
    public static double EXTRA = 3.0;

    /** Require line-of-sight for the extended poke. */
    public static boolean REQUIRE_LOS = false;

    /** Set true for quick chat debug on the server. */
    public static boolean DEBUG = false;

    private HalberdReach() {}
}
